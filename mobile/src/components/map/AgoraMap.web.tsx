import React from 'react'; import { View, Text, StyleSheet } from 'react-native'; export const AgoraMap = ({ children }: any) => <View style={styles.webMapPlaceholder}><Text style={{color:'white', fontSize: 18, fontWeight: 'bold'}}>Mapa desativado na Web</Text><Text style={{color: '#888', textAlign: 'center', marginTop: 10}}>Abra no celular via USB ou Emulador para ver o mapa original.</Text></View>; const styles = StyleSheet.create({ webMapPlaceholder: { flex: 1, backgroundColor: '#111', alignItems: 'center', justifyContent: 'center', padding: 20 }});
